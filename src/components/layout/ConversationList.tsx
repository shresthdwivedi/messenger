'use client';

const ConversationList = ({
    initialItems,
}: {
    initialItems: string[],
}) => {
  return (
    <div className="fixed">
        ConversationList
    </div>
  )
}

export default ConversationList